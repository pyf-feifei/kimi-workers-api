{
  "name": "cloudflare-kimi-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "auto-deploy": "node deploy.js"
  },
  "devDependencies": {
    "wrangler": "^3.52.0"
  }
}
